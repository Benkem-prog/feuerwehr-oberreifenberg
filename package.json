{
  "name": "feuerwehr-website",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./input.css -o ./styles/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}
